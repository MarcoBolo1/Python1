pipeline {
    agent any
stages {
  stage('Build') {
    steps {
      sh 'python3 Hello-World.py'
    }
  }

  stage('Tests') {
    steps {
      // One or more steps need to be included within the steps block.
    }
  }

  stage('Deploy') {
    steps {
      // One or more steps need to be included within the steps block.
    }
  }

  stage('Raport') {
    steps {
      // One or more steps need to be included within the steps block.
    }
  }

}
}
